# pgin
PostgreSQL internals inspector

## Releasing

To trigger a release, push a version tag:

```bash
git tag v0.1.0
git push origin v0.1.0
```

The CI workflow will build artifacts and publish a GitHub Release automatically.
